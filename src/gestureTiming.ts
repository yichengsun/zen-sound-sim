/**
 * Timing constants shared by real pointer input (input/gestures.ts) and
 * synthesized virtual-visitor phrases (virtual/phrases.ts), so both
 * produce gesture rhythms that read as the same kind of touch.
 */
export const PRESS_MS = 280;
export const HUG_MS = 2500;
export const MOVE_THRESHOLD = 14;
export const TAP_WINDOW_MS = 1600;
export const RAPID_COUNT = 3;
